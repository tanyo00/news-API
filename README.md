Our API allows us to POST, GET, DELETE news and also filter and sort the results.

- npm install (to install all dependencies included in the project)

- npm run devStart (nodemon start)

- npm start (node start)
